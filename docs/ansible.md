# Requirements
1. Install required collections via ansible-galaxy
`ansible-galaxy collection install -r requirements.yml`
2. Change **remote_user** and **private_key_file** in **ansible.cfg** to match your enviroment